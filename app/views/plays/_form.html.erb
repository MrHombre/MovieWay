<%= simple_form_for @play do |f| %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt => "Genres") %>
  <%= f.input :title, label: "Movie/Play Title" %>
  <%= f.input :director %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>